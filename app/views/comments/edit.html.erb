<h1>Edition du commentaire</h1>

<%= form_with model: @comment do |form| %>

<%= form.label :anonymous_commentor %>
<%= form.text_field :anonymous_commentor %>

<%= form.label :body %>
<%= form.text_field :body %>

<%= form.submit %>

<% end %>

<p>
<a href="<%= comment_path(@comment.id) %>" class="btn btn-danger" data-confirm="Etes-vous sûr de supprimer ce commentaire ?" data-method="delete">Supprimer le Commentaire</a>
</p>
